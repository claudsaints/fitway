import { ActivityIndicator } from "react-native";
import { Container } from ".";



export default function Loading(){
    return(
        <Container >
            <ActivityIndicator/>
        </Container>
    );
}